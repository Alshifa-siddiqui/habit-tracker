# Specify Python version if needed
python==3.12

# Database
sqlite3  # Built-in with Python, but listed for clarity

# UI (if using Tkinter)
tk  # Required only if using a GUI

# Data Analysis & Visualization
numpy~=1.26.4
pandas~=2.1.4
matplotlib~=3.9.2

# Testing & Code Coverage
pytest~=7.4.4
coverage~=7.4.3  # For measuring test coverage

# Logging & Debugging (Optional)
logging  # Built-in, no need to install separately

# User Input Handling (If needed)
questionary~=2.0.1  # Helps with CLI interactive prompts
