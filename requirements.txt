# Runtime dependencies
numpy~=1.26.4          # used by analytics (heatmap grid, bar positions)
matplotlib~=3.9.2      # charts, heatmap, progress plots

# Testing
pytest~=7.4.4
coverage~=7.4.3

# NOTE: sqlite3, tkinter, datetime, csv, json and logging are part of the
# Python 3.12 standard library and are NOT installed via pip.
